# 이어서 작업 — 핸드오프 프롬프트

> 새 세션(새 터미널)을 열고, 아래 "복붙용 프롬프트" 블록을 그대로 복사해 붙여넣으면 이어서 진행된다.
> 이 파일 자체가 현재 상태 스냅샷이다. 작성: 2026-07-24, 갱신: 2026-07-30.

---

## ✅ 2026-07-29 완료 — F-20 위험구간 8종 전부 마무리 (통신·바 개점시각 제외)

**교훈**: 직전 세션(`0e597cd` "뭔가 커밋이 이상해졌다")이 `data/albergues.ts` 주석에 "PARISH·MONASTERY·PRIVATE 239곳 예약 방법을 개별 조사해 채웠다"고 적었는데, **실제 데이터엔 하나도 안 채워져 있었다**(전부 UNKNOWN). 토큰이 떨어져 계획만 주석으로 남기고 실행은 못 한 것으로 추정. → **"했다"고 적기 전에 실제로 파일에 반영됐는지 반드시 재확인할 것.**

- `0e597cd`의 나머지 부분(`data/forks.ts` 갈림길 거리, `roadShareRatio`, F-20의 ROAD_WALKING·WINTER_RISK·STAMP)은 검증 완료.
- **reservation 239곳 재조사**: 백그라운드 에이전트(격리 worktree)로 Gronze.com 개별 페이지 "Admite reserva" 절을 실제 조사. 세션 한도로 한 번 중단(73곳 지점) → 리셋 후 재개 → 완료. **235/239 확인**(PHONE 81·WHATSAPP 5·ONLINE 132·NONE 17), 4곳은 근거를 못 찾아 UNKNOWN 유지. → 커밋 `176e046`.
- **F-20 그늘 없음(EXPOSED)**: 백그라운드 에이전트로 Gronze.com 공식 33구간 "Al Loro" 페이지 전수 확인. **7구간**에서 명시적 서술 발견해 `data/exposed_stretches.ts` 신설 + `split.ts` `buildHazards()`에 판정 추가. 통신 두절(NO_SIGNAL)은 카미노 프란세스 관련 체계적 출처가 없어서, 바 개점 시각(`Waypoint.opensAt`)은 소도시 영업시간이 계절마다 바뀌고 안정적 출처가 없어서 **의도적으로 구현 안 함**(추측 방지, 규칙 1). → 커밋 `6e7d391`.
- 두 작업 모두 격리 worktree에서 진행 후 메인 체크아웃으로 복사·재검증(`tsc` 클린·`vitest` 78/78·`npm run build` 184p)했고, 완료된 worktree 2개는 정리(`git worktree remove` + 브랜치 삭제)했다. **둘 다 커밋·푸시 완료.**

### F-20 최종 상태
- 세요 위치·차도 병행·겨울 위험·숙박 예약 상태·그늘 없음: ✅ 전부 완료
- **통신 두절·첫 바 개점 시각**: 신뢰할 출처가 없어 보류로 확정(추후 실측 GPX·통신사 커버리지 데이터 등 새 출처가 생기기 전까지 재시도 의미 없음)

---

## ✅ 완료 — 웹 트랙 Phase 1 (P0~P7)

재현 불필요. 상세는 `DEVLOG.md` 초반 항목들.

- 실측 경로·고도 파이프라인(OSM+EU-DEM) → `data/towns.ts`(82) · `data/profiles.ts`(81)
- F-01 구간 분할·부상 위험 엔진(`lib/planner/`)
- 시그니처 컴포넌트(모호온·고도단면·구간카드·위험게이지)
- `/plan` 계산기 · `/plan/print` 인쇄 · `/town·/stage·/route` SEO 175p
- P7 계측: Vercel Analytics · `/api/health` · `/privacy` · 선택적 이메일(Resend)

## ✅ 완료 — 배포 전 점검 + 테스트 인프라

- 배포 전 최종 점검(tsc·lint·test·build·보안 검토) 통과. 규칙 9(본문 17px) 위반 8개 파일 수정.
- Playwright E2E 1개(일정 생성→공유→복원) + **axe-core 접근성 자동 검사**(화면 템플릿 10종, critical/serious **0건**) 추가 — `06_개발가이드.md` "테스트 전략"이 정한 CI 파이프라인(tsc→vitest→E2E→axe-core→build) 전부 구현.

## ✅ 완료 — 문서·주석을 쉬운 말로, 확정 날짜 제거

- CLAUDE.md·README·DEVLOG·코드 주석 44개 파일·docs/markdown 11개(약 12,700줄) 전수 확인. 전문용어에 괄호 설명 추가(카미노 스페인어 용어는 규칙 5 대상이라 제외).
- 판단 게이트 표기를 "2026년 9월" 같은 확정 날짜 대신 "인터뷰 후" 식 순서·조건부로 통일(CLAUDE.md·00·05·04·사업계획서 등).

## ✅ 완료 — Phase 2 게이트 재해석 + 첫 데이터 착수

**⚠️ 가장 중요한 변화**: "실측 도보 완료 전까지 Phase 2 착수 안 함"이 폐기됐다. "실측 도보"(직접 걷기)와 "데이터를 지어내지 않는다"(규칙 1)는 원래 별개인데 하나로 묶여 있었을 뿐이다.

- **새 조건**: 출처가 있는 데이터(가이드북·공식 사이트 등)면 지금 착수 가능. `source: 'FIELD'|'GUIDEBOOK'|'ESTIMATED'` 필드로 정직하게 표시한다(어느 정도 확실한지 구분).
- **여전히 막힌 것**: 정밀 지오메트리(GPX로만 얻는 거리·오르막/내리막), 안개 지도 랜드마크 1인칭 콘텐츠, 커뮤니티 신뢰("걸어보지 않은 팀이 만든 앱은 즉시 판별된다"). 이건 데이터 정확성이 아니라 별개 이유라 그대로 유효 — 단, **창업팀이 직접 걷지 않고 필드 테스터(어차피 갈 예정인 순례자)를 모집하는 대안**을 05 문서 6.0절에 신설(아직 결정 안 함).
- **`data/forks.ts` 신설**: 갈림길 11곳 구조(변형 이름·경유 마을·계절 폐쇄·traits). 전부 `source: 'GUIDEBOOK'`. 정밀 수치(거리·ascent·descent·roadShareRatio)는 `null` — OSM에 전용 relation이 없어서(Overpass로 확인) 메인 루트처럼 파이프라인을 못 돌린다.
- **`data/access.ts` + `/tools/access` 화면 신설**: 접근 교통(F-24) 4개 경로(파리·비아리츠·팜플로나 성수기/비수기), 무료 도구 4번째로 완료. `estimatedCostKrw`의 항공권 몫은 재검색 안 한 대략치라고 코드·화면에 명시(출처 정직성 보완).
- **`docs/markdown/B_지도데이터.md` 신설**: 경로(OSM+Overpass)·고도(EU-DEM+Open Topo Data)·마을 좌표(km 비율 보간)를 어떤 API로 가져와 무엇을 계산했는지 쉽게 설명. IGN 5m를 왜 안 썼는지 비교표 포함.
- **06 문서에 "지도·내비게이션 백엔드 아키텍처(Phase 3+, 착수 전)" 절 신설**: MapLibre GL·PMTiles·GraphHopper·PostGIS·Photon 등 레이어 선택 + 오프라인 필수/온라인 보너스 분리 원칙(구글 Places 실시간 호출은 규칙 10과 충돌하므로 보조 정보로만).
- 완주 보상 브레인스토밍: 안개 지도에 스팟별 도착 시각 표시(확정, 새 스키마 불필요, `FogState.revealedAt` 활용), 여정 타임랩스·고도단면포스터(후보, 미채택), 캐릭터 도입 여부(결정 보류, v5.1 제외 결정 유지 중).
- **환경 메모**: 이 환경에서 Overpass API에 Python `urllib`은 막히고(HTTP 406) `curl`은 통과한다 — 요청 헤더/TLS 차이로 추정. 짧은 시간에 여러 번 부르면 429(rate limit) 걸리니 호출 사이 10~15초 이상 띄울 것.

## ✅ 완료 — 알베르게 데이터 283곳 (81/82 마을)

**Phase 2에서 가장 크고 중요한 항목("침대 확보 불안이 1번 문제") 착수 완료.**

- Gronze.com 프랑스 길 표준 구간(etapa) **33개 페이지 전수 조사**(WebFetch). towns.ts에 있는 마을만 추출, 없는 작은 중간 마을·산장은 제외(forks.ts와 같은 원칙).
- Hostal·Hotel·Pensión·Casa Rural는 제외하고 진짜 "Albergue"(순례자 전용 숙소)만 남김. "일시 휴업" 명시된 곳도 제외.
- **⚠️ 갈리시아(O 세브레이로~산티아고) 공립은 `XUNTA`, 그 앞은 `MUNICIPAL`로 정확히 구분** — CLAUDE.md "숙소 자주 틀림" 표가 경고하는 실수를 실제로 피해서 반영했다.
- `data/albergues.ts` 283개 레코드. `priceEur`는 도미토리 최저가 참고치 하나만, `beds`·`reservation`·`contact`·`openFrom/openTo`·`hasDryer` 등은 이번 조사 범위 밖이라 `null`/`UNKNOWN`.
- `lib/geo.ts`에 `getAlbergues()`·`ALBERGUE_TYPE_LABEL` 추가, `app/town/[slug]/page.tsx` 숙소 섹션을 실제 목록으로 교체(오바노스만 검색 결과 자체가 없어 기존 플레이스홀더 유지).
- schema.ts `Albergue.source`에 `'GUIDEBOOK'` 추가(RouteVariant·AccessRoute와 통일).
- **검수에서 실수 발견·수정**: 트리아카스텔라에 사리아 데이터를 잘못 복붙 — "모든 town id가 최소 1개 albergue는 있는지" 스크립트로 대조하다 발견, 재조사로 수정.

`npm run build` → **182 정적 페이지**(기존 town 페이지 내용만 강화, 페이지 수 동일), tsc·eslint clean, vitest 30/30.

## ✅ 완료 — F-20 일자별 상세 (부분 구현)

**새 조사 없이, 이미 가진 데이터(town.services·profiles.ts)만으로 되는 순수 코딩 작업.**

- `lib/planner/split.ts`에 `buildWaypoints()`(거점: 식수·약국·ATM·짐배송)·`buildHazards()`(위험구간: 급내리막·물없음) 추가, `Stage.waypoints`/`hazards`를 실제로 채움(이전엔 항상 빈 배열이었음).
- `components/StageCard.tsx`에 `<details>` 펼침으로 "일자별 상세 보기" 추가 — JS 없이도 서버 렌더된 그대로 동작.
- 03 문서 F-20 원래 스펙(첫 바 개점 시각·세요 위치·숙박 예약 상태·그늘/차도/통신 위험)의 상당수는 그 데이터 자체가 없어서 **의도적으로 안 채웠다** — 지어내지 않았다는 뜻.
- 테스트 6개 추가(총 36/36).

`npm run build` → **182 정적 페이지**, tsc·eslint clean, vitest 36/36. `/plan` SSR 확인 완료.

## ✅ 완료 — F-04 전화·WhatsApp 예약 스크립트 생성기

**새 조사 없이, 일반 스페인어 관광 회화(고정 템플릿)만으로 되는 순수 콘텐츠+코딩 작업.**

- `lib/phrasebook.ts`(순수 함수) — 6개 상황(알베르게 전화·WhatsApp·약국·빈대 신고·응급·식당)에
  인원(1~9명)·시각(0~23시)·이름(선택)만 채워 스페인어·한글 발음·뜻 3단 문장을 생성.
- **규칙 11 준수**: 약국·응급·빈대는 "도움·추천 요청"까지만 — 약품명·처치법 없음. 테스트에
  금칙어(이부프로펜·소염제·파라세타몰·아스피린·연고·붕대) 자동 감시 추가.
- `app/tools/phrases/page.tsx` + `PhraseControls.tsx`(URL 쿼리스트링 상태) + `CopyButton.tsx`
  (범용 클립보드 복사, `ShareButton.tsx`와 같은 패턴). `ToolNav`·`sitemap.ts` 반영.
- 테스트 18개 추가(총 54/54).

`npm run build` → **183 정적 페이지**, tsc·eslint clean. `/tools/phrases` 4개 상황 SSR 확인 완료.

## ✅ 완료 — F-05 짐배송 비용 비교 (리스크 수치화는 보류)

**`/tools/cost`와 같은 독립형 계산기 패턴(범위 출력, lib/planner 밖 인라인 순수 함수).**

- `app/tools/luggage/page.tsx` + `LuggageControls.tsx` — 배송 안 함/힘든 날만/매일 3개
  시나리오의 배송비+숙박비 차액+총 추가 비용을 범위로 계산. "공립은 짐 배송 불가"(기존
  도메인 사실) → 배송 쓰는 날은 사립 강제 전환.
- 배송비 €5~7/구간은 WebSearch로 재확인(Correos Paq Mochila·Jacotrans). 숙박비는
  `/tools/cost`의 조사치(공립 €8~10·사립 €12~25) 재사용.
- **03 문서 원안의 "부상 위험 점수 62→41→28"은 의도적으로 구현 안 함** — 근거 없는
  가중치를 또 만드는 게 규칙 11 정신에 어긋난다고 판단, 정성적 문장만 안내.
- 재확인 불필요한 계산기라 별도 테스트 파일은 안 만듦(cost/pack/timeline과 같은 전례).

`npm run build` → **184 정적 페이지**, tsc·eslint clean. `/tools/luggage` SSR 확인 + 수기 검산 일치.

## ✅ 완료 — F-26 계획된 이동수단 (부르고스~레온 메세타 구간)

- `data/transit.ts` 신설 — WebSearch로 실제 노선 조사: 부르고스~레온(ALSA 버스 약 1시간
  40분·€15~35, Renfe Alvia 기차 약 1시간 20분·€20~40), 사아군~레온 부분구간(ALSA 버스
  약 55분·€6~9). **03 문서 원안 "버스 약 3시간"은 실측 전 추정치였음을 확인, 정정.**
- `lib/schema.ts`에 `TransitOption` 신설(조사된 실제 노선), `lib/geo.ts`에
  `findTransitOptions()` 추가(없으면 빈 배열 — 지어내지 않음), `lib/url.ts`의
  `parseSkips()`가 이걸로 실제 mode·시간·비용을 채움.
- `PlanControls.tsx`의 기존 단일 체크박스를 3지 라디오(전부 걷기/사아군만/전체)로 교체,
  실제 소요시간·요금·단축거리 노출. 조사 안 된 임의 구간은 여전히 일반 문구로 폴백.
- 새 라우트 없이 `/plan` 자체를 강화하는 형태라 사이트맵·ToolNav 변경 없음.

`npm run build` → **184 정적 페이지**(신규 라우트 없음), tsc·eslint clean. `/plan?skip=burgos~leon`·
`skip=sahagun~leon` SSR 확인 완료.

## ✅ 완료 — data/albergues.ts beds 보강(280/283곳) + F-02 혼잡 추정

**사용자가 "전체 283곳 다 조사"를 선택 → 백그라운드 에이전트로 위임(세션 한도로 중단·재개 1회) → 완료 후 F-02 본 기능 구현.**

- `data/albergues.ts`: Gronze.com **개별 알베르게 상세 페이지**(구간 요약 페이지가
  아니라)에서 도미토리별 침대 수를 합산해 beds 채움. 283곳 중 280곳 확보, 3곳은
  페이지 없음/방 단위 표기라 null 유지(카스트로헤리스 Espacio Interior, 카카벨로스
  Saint James Way, 트라바델로 Camino y Leyenda).
- **데이터 불일치 발견**: `data/towns.ts`의 기존 `beds`(출처 불명 레거시)와 새 조사
  합계가 81개 마을 중 21곳에서 50% 넘게 차이(몬테 도 고소 400→1120, 부르고스
  350→136 등). **사용자에게 물어 "새 조사치로 towns.ts 교체"를 선택받아 반영**
  (오바노스만 albergues.ts에 데이터가 없어 레거시 값 유지). `REST_TOWN_MIN_BEDS`·
  `FEW_BEDS` 임계값 로직에 영향 있으나 재검증 결과 회귀 없음.
- `lib/planner/congestion.ts`(신설): 03 문서 원안의 정밀 수요 공식(계절/요일/구간/
  특수일 계수)은 기준 순례자 수 실측치가 없어 **구현하지 않았다** — 대신 마을 실측
  침대 수·사리아 이후·성수기(5~10월, 83%, WebSearch 확인)·9월 최성수·부활절
  (그레고리력 계산, 지어낸 값 아님)·성 야고보 축일(산티아고 인근)·산 페르민
  (팜플로나 인근)·2027 성년만으로 3단계(여유/서두르세요/만실 가능성 높음) 판정.
- `lib/schema.ts`에 `CongestionLevel`/`CongestionInfo`/`Stage.congestion` 추가.
  기존에 늘 `null`이던 `Stage.date`도 이번에 실제로 채움(`split.ts`의
  `finalizeDatesAndCongestion()` — 휴식일 삽입으로 dayNo 재부여된 *이후*에만
  계산 가능해서 별도 후처리 단계로 분리, 순서 실수하면 날짜가 밀리는 버그였음).
- `lib/geo.ts`에 `totalBedsForTown()` 추가. `StageCard`에 등급별 색상 표시(LOW=moss,
  HIGH=vino, flecha 노란색은 안 씀 — 길 안내 전용 규칙).
- 테스트 21개 추가(congestion.test.ts 16개 — 부활절 실제 날짜 2024~2028로 검증,
  split.test.ts 배선 5개). 총 76/76.

`npm run build` → **184 정적 페이지**, tsc·eslint clean. `/plan?start=sarria&sd=2026-09-01`
SSR 확인 — "서두르세요"·"만실 가능성 높음"과 실제 근거 문구 노출 확인.

---

## ✅ 2026-07-30 완료 — F-19 갈림길 화면(ForkPicker)

**배경**: `PlanInput.variantChoices`는 스키마에만 있고 실제로 어디서도 안 읽히던
필드였다. `data/forks.ts`(갈림길 11곳 구조·거리)는 있는데 화면이 없어서
"데이터+화면 세트" 원칙에 어긋난 상태였다.

- `lib/planner/forks.ts`(신설): fork가 하루 구간(Stage)에 **완전히** 들어갈 때만
  판정하는 순수 함수(`forksFullyInStage`). 경계 걸치면 손대지 않는다(규칙 1).
  나폴레옹 겨울 폐쇄(11-01~03-31) 판정(`isVariantClosedOn`)도 포함.
- `lib/planner/split.ts`의 `applyVariantOverrides`: 선택한 variant의 `distanceKm`
  (있으면)으로 그 Stage의 거리·시간을 재계산. `ascent`/`descent`는 변형 쪽
  실측이 없으면(`null`) 본선 값을 그대로 보존.
- `lib/url.ts`: `v=forkId~variantId` 쿼리, `withVariantChoice()`로 링크 href를
  서버 렌더 시점에 계산(JS 없이도 클릭 동작).
- `components/ForkPicker.tsx`(신설): `/plan` 각 날짜 카드 안에 렌더. 거리·특징·
  주의·폐쇄 경고 표시, 어느 쪽도 권하지 않음.
- `StageCard.tsx`의 `HAZARD_ICON`에 `ROAD_WALKING`/`WINTER_RISK`/`EXPOSED` 한글
  라벨 추가(전에 영문 그대로 노출되던 버그 발견해서 같이 고침).
- `npm run dev` + Playwright(Chrome 채널, `chromium-cli` 없어서 대체)로 실제
  클릭까지 검증: 발카를로스 선택 → 25.7km→23.6km, 콤포스텔라 총거리
  773→771km까지 연쇄 갱신, 겨울 폐쇄 경고 정상 노출, 콘솔 에러 없음.
- 테스트 27개 추가. `tsc`·`vitest`(102/102)·`eslint`·`next build`(184p) 통과.
- **남은 것**: forks.ts 23개 variant 중 7개(에우나테·몬테후라·gamonal·부르고스
  강변길·비아트라야나·발투이예 등)는 `distanceKm`이 `null`이라 정보만 뜨고
  거리 반영은 안 됨 — data 자체의 한계.

---

## 🗓️ 다음 세션에서 할 일

**F-04→F-05→F-26→F-02→F-20(전부)→F-19까지 전부 끝났다.** 사용자가 2026-07-28에
정한 원래 순서 밖의 다음 후보는 아직 확정 안 됨 — 아래 중 골라서 물어볼 것.

| 순서 | 후보 | 내용 | 상태 |
|---|---|---|---|
| 1~4 | F-04·F-05·F-26·F-02 | — | ✅ 완료 |
| 5 | F-20 전체(세요·차도·겨울·예약·그늘) | 통신 두절·바 개점시각만 출처 없어 영구 보류 | ✅ 완료 |
| 6 | F-19 갈림길 화면(ForkPicker) | 위 2026-07-30 섹션 참고 | ✅ 완료 |
| 7 | **다음 후보(미정)** | F-24 Day 0(접근교통) 일정 통합 / F-21 Plan B(각 날짜 카드 접힘) / 그 외 03문서 F-12~F-25 중 Phase 2 범위 | 다음 세션 시작 시 사용자에게 먼저 물어볼 것 — 이번엔 "다시 물어보지 말고"가 아니라 진짜 선택지가 여럿이라 확인 필요 |

**F-24·albergues.ts·phrases·luggage·transit·congestion·forks(F-19)처럼 "데이터(필요시) + 화면"을 한 세트로 끝내는 패턴을 따른다.** 데이터만 만들고 화면 없이 남기지 않는다.

---

## 📋 복붙용 프롬프트 (다음 세션 시작용)

```
카미노 프로젝트. 웹 트랙 Phase 1은 끝났고, Phase 2 게이트도 재해석 완료
(실측 도보 없이도 "출처 있는 데이터"면 착수 가능 — CLAUDE.md 참고).
data/forks.ts(갈림길 구조+거리 16/23개), data/access.ts + /tools/access(접근 교통),
data/albergues.ts + /town/[slug](알베르게 283곳, beds 280곳·reservation 279곳 확보),
F-20 일자별 상세(거점·위험구간 8종 중 6종 실제 데이터로 산출, 나머지 2종은
출처 없어 영구 보류), F-19 갈림길 화면(components/ForkPicker.tsx, /plan에 통합,
variantChoices 실제로 배선됨), F-04 예약 문장 생성기(lib/phrasebook.ts),
F-05 짐배송 비용 비교(/tools/luggage), F-26 계획된 이동수단(data/transit.ts),
F-02 혼잡 추정(lib/planner/congestion.ts)까지 만들었다.

사용자가 2026-07-28에 확정한 순서(F-04→F-05→F-26→F-02→F-20→F-19)는 전부 끝났다.
**다음 후보가 확정 안 됐으니, 이번엔 바로 시작하지 말고 먼저 사용자에게 뭘
할지 물어볼 것**(F-24 Day 0 통합 / F-21 Plan B / 그 외 03문서 기능 중 선택).

[먼저 할 것]
1. CLAUDE.md, README.md, DEVLOG.md 최신 항목, 이 파일(이어서작업.md) 읽기
2. npx tsc --noEmit / npm test / npm run lint / npm run build 통과 확인
3. git log --oneline -10 / git reflog -10 도 확인 — 이 저장소는 여러 세션이
   동시에 작업할 때가 있다(2026-07-29 실제 충돌 사례 있음). "완료"라고 적힌
   항목도 실제 코드에 반영됐는지 grep으로 재확인할 것(과거 0e597cd처럼 주석만
   "했다"고 적고 실제 파일엔 안 채워진 사례가 있었다).

[진행 방식 — 이미 검증된 패턴]
- 실제 자료는 WebSearch/WebFetch로 조사하고 출처 URL을 파일 상단 주석에 남긴다
- 스키마에 source: 'FIELD'|'GUIDEBOOK'|'ESTIMATED' 필드가 있는지 확인하고, 없으면
  RouteVariant/AccessRoute처럼 추가한다 (Albergue는 이미 있음)
- 정밀 수치(거리·오르막/내리막 등)를 가이드북에서 못 구하면 지어내지 말고 null로
  남긴다 — 규칙 1·3
- "estimatedCostKrw"처럼 여러 출처가 섞인 값은 어디까지 직접 확인했는지 주석으로
  명확히 가른다 (data/access.ts 참고 사례)
- 데이터만 만들고 끝내지 말고, 화면(/tools/... 또는 기존 화면에 통합)까지 세트로
  완성한다 (F-24 패턴)
- 문서(CLAUDE.md·00·03 등) 반영 + DEVLOG 기록 + docs/markdown 고쳤으면
  python scripts/regen_docs.py로 docx·문서뷰어 재생성

[하지 말 것]
- 정밀 지오메트리(GPX 필요한 것)·안개 지도 1인칭 콘텐츠는 여전히 실측 전까지 보류
- 서비스명 확정을 임의로 하지 마라. config/brand.ts 그대로 둔다
- 응답은 한글로, 전문용어는 괄호로 쉽게 설명
```

---

## 🔑 계속 알아야 할 것

| # | 내용 |
|---|---|
| 1 | 데이터 스크립트는 Windows에서 `python3`이 아니라 `python`(python3은 MS Store 가짜 실행 파일) |
| 2 | 좌표 원본 `data/geometry/`는 gitignore 대상. 좌표를 `profiles.ts`에 넣지 않는다(ODbL 파생 DB 배포 회피) |
| 3 | 고도 출처는 EU-DEM 25m(실제)이지 IGN 5m 아니다 — `source: 'OSM+MDT'`는 스키마에 IGN용으로 예약만 돼 있음, 실사용은 `'OSM+EUDEM'` |
| 4 | 이메일 수집은 Resend(전용 SDK 없이 fetch로 호출). 실제 서비스하려면 `RESEND_API_KEY`·`RESEND_AUDIENCE_ID` 발급 필요 — 아직 없음 |
| 5 | `/privacy` 문의처는 서비스명 미확정이라 placeholder. 이름 정해지면 채워야 함 |
| 6 | `.md`(CLAUDE.md·docs/markdown/)가 정본. 고치면 `python scripts/regen_docs.py`로 docx·문서뷰어 재생성 필요 |
| 7 | Overpass API는 이 환경에서 `curl`로만 안정적으로 접속됨(파이썬 `urllib`은 막힘). 호출 간 10~15초 이상 간격 필요(rate limit) |
| 8 | Phase 2 데이터 작업 시 `source`/`checkedAt`(또는 `verifiedAt`) 필드 채우기, 정밀 수치 없으면 `null` — `data/forks.ts`·`data/access.ts`·`data/albergues.ts`가 표준 사례 |
| 9 | 배포 콘솔 작업(아직 안 함): Vercel에 `NEXT_PUBLIC_SITE_URL`·`RESEND_API_KEY`·`RESEND_AUDIENCE_ID` 설정 |
| 10 | Gronze.com(`gronze.com/etapa/[from]/[to]`)이 알베르게 조사에 제일 좋은 출처 — 구간별로 마을 여러 개를 한 번에 준다. WebFetch 프롬프트에 "list ONLY these towns" + "pilgrim hostels, not regular hotels"를 꼭 넣을 것(안 넣으면 호텔·펜션까지 섞여 나옴) |
| 11 | 여러 town을 한 번에 조사할 때 사람이 실수로 다른 마을 데이터를 복붙할 수 있다 — 마지막에 "모든 townId가 최소 1개 데이터는 있는지" 스크립트로 자동 대조할 것(오늘 트리아카스텔라 실수를 이렇게 잡았음) |

메모리(MEMORY.md)에 세션별 결정 기록 있음 — `phase2_gate_reinterpreted`·`response_language_korean`·`gate_dates_condition_based`가 최신.
