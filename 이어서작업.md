# 이어서 작업 — 핸드오프 프롬프트

> 새 세션(새 터미널)을 열고, 아래 "복붙용 프롬프트" 블록을 그대로 복사해 붙여넣으면 이어서 진행된다.
> 이 파일 자체가 현재 상태 스냅샷이다. 작성: 2026-07-24, 갱신: 2026-07-27.

---

## ✅ 완료 — 웹 트랙 Phase 1 (P0~P7)

재현 불필요. 상세는 `DEVLOG.md` 초반 항목들.

- 실측 경로·고도 파이프라인(OSM+EU-DEM) → `data/towns.ts`(82) · `data/profiles.ts`(81)
- F-01 구간 분할·부상 위험 엔진(`lib/planner/`)
- 시그니처 컴포넌트(모호온·고도단면·구간카드·위험게이지)
- `/plan` 계산기 · `/plan/print` 인쇄 · `/town·/stage·/route` SEO 175p
- P7 계측: Vercel Analytics · `/api/health` · `/privacy` · 선택적 이메일(Resend)

## ✅ 완료 — 배포 전 점검 + 테스트 인프라

- 배포 전 최종 점검(tsc·lint·test·build·보안 검토) 통과. 규칙 9(본문 17px) 위반 8개 파일 수정.
- Playwright E2E 1개(일정 생성→공유→복원) + **axe-core 접근성 자동 검사**(화면 템플릿 10종, critical/serious **0건**) 추가 — `06_개발가이드.md` "테스트 전략"이 정한 CI 파이프라인(tsc→vitest→E2E→axe-core→build) 전부 구현.

## ✅ 완료 — 문서·주석을 쉬운 말로, 확정 날짜 제거

- CLAUDE.md·README·DEVLOG·코드 주석 44개 파일·docs/markdown 11개(약 12,700줄) 전수 확인. 전문용어에 괄호 설명 추가(카미노 스페인어 용어는 규칙 5 대상이라 제외).
- 판단 게이트 표기를 "2026년 9월" 같은 확정 날짜 대신 "인터뷰 후" 식 순서·조건부로 통일(CLAUDE.md·00·05·04·사업계획서 등).

## ✅ 완료 — Phase 2 게이트 재해석 + 첫 데이터 착수

**⚠️ 가장 중요한 변화**: "실측 도보 완료 전까지 Phase 2 착수 안 함"이 폐기됐다. "실측 도보"(직접 걷기)와 "데이터를 지어내지 않는다"(규칙 1)는 원래 별개인데 하나로 묶여 있었을 뿐이다.

- **새 조건**: 출처가 있는 데이터(가이드북·공식 사이트 등)면 지금 착수 가능. `source: 'FIELD'|'GUIDEBOOK'|'ESTIMATED'` 필드로 정직하게 표시한다(어느 정도 확실한지 구분).
- **여전히 막힌 것**: 정밀 지오메트리(GPX로만 얻는 거리·오르막/내리막), 안개 지도 랜드마크 1인칭 콘텐츠, 커뮤니티 신뢰("걸어보지 않은 팀이 만든 앱은 즉시 판별된다"). 이건 데이터 정확성이 아니라 별개 이유라 그대로 유효 — 단, **창업팀이 직접 걷지 않고 필드 테스터(어차피 갈 예정인 순례자)를 모집하는 대안**을 05 문서 6.0절에 신설(아직 결정 안 함).
- **`data/forks.ts` 신설**: 갈림길 11곳 구조(변형 이름·경유 마을·계절 폐쇄·traits). 전부 `source: 'GUIDEBOOK'`. 정밀 수치(거리·ascent·descent·roadShareRatio)는 `null` — OSM에 전용 relation이 없어서(Overpass로 확인) 메인 루트처럼 파이프라인을 못 돌린다.
- **`data/access.ts` + `/tools/access` 화면 신설**: 접근 교통(F-24) 4개 경로(파리·비아리츠·팜플로나 성수기/비수기), 무료 도구 4번째로 완료. `estimatedCostKrw`의 항공권 몫은 재검색 안 한 대략치라고 코드·화면에 명시(출처 정직성 보완).
- **`docs/markdown/B_지도데이터.md` 신설**: 경로(OSM+Overpass)·고도(EU-DEM+Open Topo Data)·마을 좌표(km 비율 보간)를 어떤 API로 가져와 무엇을 계산했는지 쉽게 설명. IGN 5m를 왜 안 썼는지 비교표 포함.
- **06 문서에 "지도·내비게이션 백엔드 아키텍처(Phase 3+, 착수 전)" 절 신설**: MapLibre GL·PMTiles·GraphHopper·PostGIS·Photon 등 레이어 선택 + 오프라인 필수/온라인 보너스 분리 원칙(구글 Places 실시간 호출은 규칙 10과 충돌하므로 보조 정보로만).
- 완주 보상 브레인스토밍: 안개 지도에 스팟별 도착 시각 표시(확정, 새 스키마 불필요, `FogState.revealedAt` 활용), 여정 타임랩스·고도단면포스터(후보, 미채택), 캐릭터 도입 여부(결정 보류, v5.1 제외 결정 유지 중).
- **환경 메모**: 이 환경에서 Overpass API에 Python `urllib`은 막히고(HTTP 406) `curl`은 통과한다 — 요청 헤더/TLS 차이로 추정. 짧은 시간에 여러 번 부르면 429(rate limit) 걸리니 호출 사이 10~15초 이상 띄울 것.

`npm run build` → **182 정적 페이지**, tsc·eslint clean, vitest 30/30, playwright 11/11(E2E 1 + 접근성 10).

---

## 🗓️ 다음 세션에서 할 일 — 둘 중 미정

Phase 2 게이트가 풀렸으니 "출처 있는 데이터"로 계속 채울 수 있다. 다음 후보 둘 중 **아직 어느 쪽부터 할지 정하지 않았다** — 세션 시작할 때 사용자에게 먼저 물어볼 것.

| 후보 | 내용 | 규모 |
|---|---|---|
| **알베르게 실데이터** | 마을 82곳의 숙소(유형·요금·예약방법·개방기간). `data/albergues.ts` 신설 | **큼** — 마을당 여러 곳, 가이드북 조사량이 forks.ts·access.ts보다 훨씬 많음 |
| **F-26 계획된 이동수단** | 메세타(부르고스→레온) 버스 등 노선·요금 | 중간 — access.ts와 비슷한 규모로 예상 |

그 외 이미 구조가 있는 남은 Phase 2 항목: F-02(혼잡 추정), F-04(전화 예약 스크립트), F-05(짐배송 판단, 알베르게 데이터에 종속), F-20(일자별 상세), F-22(체크리스트).

**F-24처럼 "데이터 파일 + 화면"을 한 세트로 끝내는 패턴을 따른다.** 데이터만 만들고 화면 없이 남기지 않는다.

---

## 📋 복붙용 프롬프트 (다음 세션 시작용)

```
카미노 프로젝트. 웹 트랙 Phase 1은 끝났고, Phase 2 게이트도 재해석 완료
(실측 도보 없이도 "출처 있는 데이터"면 착수 가능 — CLAUDE.md 참고).
data/forks.ts(갈림길 구조), data/access.ts + /tools/access(접근 교통)까지 만들었다.

[먼저 할 것]
1. CLAUDE.md, README.md, DEVLOG.md 최신 항목, 이 파일(이어서작업.md) 읽기
2. npx tsc --noEmit / npm test / npm run lint / npm run build 통과 확인
3. 다음 중 뭘 할지 나(사용자)에게 먼저 물어봐:
   - 알베르게 실데이터(data/albergues.ts, 규모 큼)
   - F-26 계획된 이동수단(메세타 버스 등, access.ts급 규모)
   - 아니면 다른 것

[진행 방식 — 이미 검증된 패턴]
- 실제 자료는 WebSearch/WebFetch로 조사하고 출처 URL을 파일 상단 주석에 남긴다
- 스키마에 source: 'FIELD'|'GUIDEBOOK'|'ESTIMATED' 필드가 있는지 확인하고, 없으면
  RouteVariant/AccessRoute처럼 추가한다 (Albergue는 이미 있음)
- 정밀 수치(거리·오르막/내리막 등)를 가이드북에서 못 구하면 지어내지 말고 null로
  남긴다 — 규칙 1·3
- "estimatedCostKrw"처럼 여러 출처가 섞인 값은 어디까지 직접 확인했는지 주석으로
  명확히 가른다 (data/access.ts 참고 사례)
- 데이터만 만들고 끝내지 말고, 화면(/tools/... 또는 기존 화면에 통합)까지 세트로
  완성한다 (F-24 패턴)
- 문서(CLAUDE.md·00·03 등) 반영 + DEVLOG 기록 + docs/markdown 고쳤으면
  python scripts/regen_docs.py로 docx·문서뷰어 재생성

[하지 말 것]
- 정밀 지오메트리(GPX 필요한 것)·안개 지도 1인칭 콘텐츠는 여전히 실측 전까지 보류
- 서비스명 확정을 임의로 하지 마라. config/brand.ts 그대로 둔다
- 응답은 한글로, 전문용어는 괄호로 쉽게 설명
```

---

## 🔑 계속 알아야 할 것

| # | 내용 |
|---|---|
| 1 | 데이터 스크립트는 Windows에서 `python3`이 아니라 `python`(python3은 MS Store 가짜 실행 파일) |
| 2 | 좌표 원본 `data/geometry/`는 gitignore 대상. 좌표를 `profiles.ts`에 넣지 않는다(ODbL 파생 DB 배포 회피) |
| 3 | 고도 출처는 EU-DEM 25m(실제)이지 IGN 5m 아니다 — `source: 'OSM+MDT'`는 스키마에 IGN용으로 예약만 돼 있음, 실사용은 `'OSM+EUDEM'` |
| 4 | 이메일 수집은 Resend(전용 SDK 없이 fetch로 호출). 실제 서비스하려면 `RESEND_API_KEY`·`RESEND_AUDIENCE_ID` 발급 필요 — 아직 없음 |
| 5 | `/privacy` 문의처는 서비스명 미확정이라 placeholder. 이름 정해지면 채워야 함 |
| 6 | `.md`(CLAUDE.md·docs/markdown/)가 정본. 고치면 `python scripts/regen_docs.py`로 docx·문서뷰어 재생성 필요 |
| 7 | Overpass API는 이 환경에서 `curl`로만 안정적으로 접속됨(파이썬 `urllib`은 막힘). 호출 간 10~15초 이상 간격 필요(rate limit) |
| 8 | Phase 2 데이터 작업 시 `source`/`checkedAt` 필드 채우기, 정밀 수치 없으면 `null` — `data/forks.ts`·`data/access.ts`가 표준 사례 |
| 9 | 배포 콘솔 작업(아직 안 함): Vercel에 `NEXT_PUBLIC_SITE_URL`·`RESEND_API_KEY`·`RESEND_AUDIENCE_ID` 설정 |

메모리(MEMORY.md)에 세션별 결정 기록 있음 — `phase2_gate_reinterpreted`·`response_language_korean`·`gate_dates_condition_based`가 최신.
