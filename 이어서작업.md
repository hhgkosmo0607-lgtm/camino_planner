# 이어서 작업 — 핸드오프 프롬프트

> 집에서 새 세션(새 터미널)을 열고, 아래 "프롬프트" 블록을 그대로 복사해 붙여넣으면 이어서 진행된다.
> 이 파일 자체가 현재 상태 스냅샷이기도 하다. 작성: 2026-07-24, 갱신: 2026-07-27.

---

## ✅ 2026-07-27 완료 (웹 트랙 Phase 1, P0~P7 전부)

아래는 **끝났다.** 재현 불필요. 상세 이력은 `DEVLOG.md`.

- 실측 경로·고도 파이프라인(OSM+EU-DEM) → `data/towns.ts`(82) · `data/profiles.ts`(81)
- F-01 구간 분할·부상 위험 엔진(`lib/planner/`, 테스트 30/30)
- 시그니처 컴포넌트(모호온·고도단면·구간카드·위험게이지)
- `/plan` 계산기 · `/plan/print` 인쇄 · `/town·/stage·/route` SEO 175p · `/tools/{cost,pack,timeline}` 무료 도구
- P7 계측: Vercel Analytics 이벤트 6종 · `/api/health` · `/privacy` · 선택적 이메일 수집(Resend, 키 없으면 자동 비활성화)
- `npm run build` → **181 정적 페이지**, tsc·eslint clean

**웹 트랙 Phase 1 코드로 할 일은 이제 없다.** 다음 단계(Phase 2: 알베르게 데이터·갈림길·짐배송 비교 등)는 **실측 도보 완료 후에만** 착수한다(06 문서 "이후 단계 — 조건부", CLAUDE.md 절대 규칙 1과 직결 — 안 가본 길·안 확인한 숙소 정보를 지어내지 않기 위해서다).

---

## ✅ 2026-07-27 완료 (배포 전 최종 점검)

아래 "복붙용 프롬프트"에 있던 점검을 이 세션에서 실행했다. 상세는 `DEVLOG.md` 2026-07-27 (13).

- Windows 세션 `node_modules`에 `@vercel/analytics`·`vitest`가 없어 `tsc`/`test`가 실패했던 것 — `npm install`로 해결(코드 문제 아니었음)
- `tsc`·`test`(30/30)·`build`(181페이지) 전부 통과. `npm audit` high 12건은 `next`/`eslint` 번들 전이 의존성이라 보류
- `/api/health`·`/privacy` 정상 응답, `EmailCapture`는 `RESEND_API_KEY` 유무에 따라 정확히 토글됨
- `/api/subscribe`·`/api/health` 수동 보안 검토 — 발견 사항 없음
- **규칙 9(본문 17px) 위반 8개 파일 발견·수정**: RiskGauge 등 실제 본문 문단이 15px였던 것을 17px로 수정

**남은 건 코드가 아니라 배포 콘솔 작업**(아래 "복붙용 프롬프트" 5번 항목): Vercel에 `NEXT_PUBLIC_SITE_URL`·`RESEND_API_KEY`·`RESEND_AUDIENCE_ID` 설정 여부 확인. 그 다음은 05 문서 게이트 일정(사이트 공개 → 트래픽 4주 → 실측 도보) 대로 진행.

---

## 🗓️ 지금 이 시점에서 진짜 다음 할 일 (05 문서 "판단 게이트" 기준)

코드가 아니라 **일정** 문제다. 특정 달력 날짜가 아니라 **순서와 선행조건**으로 판단한다 (05 문서 원본엔 참고용 월이 있지만, 실행 시점은 아래 조건 충족 여부로 정한다):

| 순서 | 게이트 | 내용 | 코드 작업 필요? |
|---|---|---|---|
| 1 | 게이트 1 (인터뷰 후) | H1~H4 가설 검증(예약 판단 어려움·현장 폰 사용·부상 이탈·준비 공백) | 아니오 — 사용자 인터뷰 |
| 2 (게이트 2 선행조건) | 사이트 공개 + 트래픽 확보 시작 | 게이트 2가 "트래픽 4주 후"를 보므로, 게이트 2보다 먼저 배포돼 있어야 함 | **예 — 배포 준비** |
| 3 | 게이트 2 (트래픽 4주 관찰 후) | 월 1,500세션·네이버 비중·여행사 수수료 반응 | 아니오 — 데이터 관측 |
| 4 | **실측 도보 40일** | 숙소 120곳·GPX 전 구간·갈림길 11곳·랜드마크 19곳 실측 | 도보 후 데이터 반영 코드 |
| 5 | 게이트 3 (실측 도보 후) | 실측 데이터로 H12·H15 검증 | 도보 결과에 따라 결정 |

**따라서 다음 세션에서 코드로 할 일이 있다면 "배포 전 최종 점검"이다.** 아래 프롬프트는 그 작업용.

---

## 📋 복붙용 프롬프트 (배포 전 최종 점검)

```
카미노 프로젝트, 웹 트랙 Phase 1(P0~P7) 코드는 다 끝났어. 이제 실제 배포 전
마지막 점검을 하자.

[확인할 것]
1. npx tsc --noEmit / npm run lint / npm test / npm run build 전부 통과하는지
   (build 1차 시도가 Google Fonts 네트워크 문제로 실패할 수 있음 — 재시도하면
   보통 됨. 코드 문제 아님.)
2. CLAUDE.md 접근성 하한선(규칙 9) 재점검: 본문 17px+, 주요 수치 18px+,
   명도 대비 4.5:1, 터치 타깃 44x44px, 포커스 링 유지, 인쇄(@media print) 동작
3. /privacy, /api/health 가 실제로 정상 응답하는지
4. 이메일 수집(EmailCapture)이 RESEND_API_KEY 없을 때 안 보이는지,
   있을 때(가짜 키로도) 보이는지 (app/plan/page.tsx 하단)
5. .env.example 대로 실제 Vercel 프로젝트에 환경변수(NEXT_PUBLIC_SITE_URL 등)
   설정 여부 — 이건 배포 콘솔 작업이라 내가 직접은 못 함, 체크리스트만 정리
6. security-review 스킬로 /api/subscribe, /api/health 점검
   (입력값 검증·레이트리밋(짧은 시간에 너무 많이 요청하면 막는 장치) 필요 여부 등)

[하지 말 것]
- Phase 2 기능(알베르게 실데이터, 갈림길 forks.ts, 짐배송 비교 등)을 먼저 만들지
  마라. 실측 도보 완료 전까지는 규칙 1 위반이다.
- 서비스명 확정을 임의로 하지 마라. config/brand.ts 그대로 둔다.

작업 전에 CLAUDE.md, README.md, DEVLOG.md 최신 항목을 읽어줘.
```

---

## 🔑 계속 알아야 할 것

| # | 내용 |
|---|---|
| 1 | 이 Mac은 Node `/opt/homebrew`, 데이터 스크립트는 `python3` (Windows 시절 지침과 다름 — `dev-env-mac` 메모리 참고) |
| 2 | 좌표 원본 `data/geometry/`는 gitignore(깃에 올리지 않는 파일 목록) 대상. 좌표를 `profiles.ts`에 넣지 않는다(ODbL 라이선스상 "파생 데이터베이스 공개 의무"를 피하려고) |
| 3 | 고도 출처는 EU-DEM 25m(실제)이지 IGN 5m 아니다 — `source: 'OSM+MDT'`는 스키마에 IGN용으로 예약만 돼 있음, 실사용은 `'OSM+EUDEM'` |
| 4 | 이메일 수집은 Resend(이메일 발송 서비스)로 결정(전용 SDK(그 서비스 전용 코드 도구모음) 없이 fetch(직접 인터넷 요청 보내기)로 호출). 실제 서비스하려면 `RESEND_API_KEY`·`RESEND_AUDIENCE_ID` 발급 필요 — 아직 없음 |
| 5 | `/privacy` 문의처는 서비스명 미확정이라 placeholder 상태. 이름 정해지면 채워야 함 |
| 6 | `.md`(CLAUDE.md·docs/markdown/)가 정본. 고치면 `python3 scripts/regen_docs.py`로 docx·문서뷰어 재생성 필요 (`docx-regeneration` 메모리) |

메모리(MEMORY.md)에 각 Phase 완료 기록이 세션별로 있음 — `camino-p7-instrumentation-done`이 최신.
