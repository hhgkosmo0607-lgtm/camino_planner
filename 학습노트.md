# 학습노트 — 코딩 세션에서 나온 질문 정리

> 개발 진행하면서 궁금했던 것들을 나중에 다시 볼 수 있게 정리한 파일. 실행 계획 문서 아님, 개인 학습용.

---

## 1. "가상환경"은 왜 파이썬 venv가 아니었나

**질문**: 가상환경 만들자고 했는데 왜 Node.js 얘기가 나왔나?

**답**: "가상환경(virtual environment)"은 파이썬 생태계 용어(`venv`, `conda` 등 — 프로젝트마다 라이브러리 버전이 안 섞이게 격리하는 것)다. 이 프로젝트는 Next.js/TypeScript(자바스크립트 생태계)라서 같은 역할을 하는 게 **Node.js 프로젝트 하나(`package.json` + `node_modules`)** 다. 언어 생태계마다 "격리된 개발 환경"을 부르는 말이 다르다:

| 생태계 | 격리 단위 |
|---|---|
| Python | venv, conda |
| Node.js/JS | package.json + node_modules (프로젝트 폴더 자체가 격리 단위) |

---

## 2. Node.js가 컴퓨터에 아예 없었던 이유와 설치 방법

이 컴퓨터엔 Node.js가 설치된 적이 없었다. `nvm-windows`(Node 버전 관리자)를 깔고, 그걸로 Node LTS(24.18.0)를 설치했다.

**왜 nvm을 거쳐서 까나?** 나중에 프로젝트마다 다른 Node 버전이 필요해질 수 있어서 (예: 이 프로젝트는 24, 다른 프로젝트는 20) 버전을 자유롭게 바꿔 쓸 수 있게 하는 게 nvm의 역할이다. 파이썬의 `pyenv`와 같은 개념.

**설치 직후 왜 바로 안 됐나?** Windows는 환경변수(PATH)를 레지스트리에 저장하는데, **이미 열려 있는 터미널/프로세스는 이 변경을 자동으로 못 받는다.** 새 터미널을 열어야 반영된다. (Claude Code 도구가 쓰는 셸은 세션 내내 재사용되는 구조라 계속 직접 경로를 지정해야 했다.)

---

## 3. Next.js 15 vs 16, Tailwind v3 vs v4 — 왜 문서랑 실제가 달랐나

CLAUDE.md는 "Next.js 15"라고 적어뒀는데, 지금 `create-next-app`으로 설치하니 **Next.js 16**이 깔렸다. 문서를 쓴 시점 이후 새 버전이 나온 것뿐이다. **패키지 버전은 시간이 지나면 문서보다 항상 앞서 나간다** — 새 프로젝트를 시작할 때는 문서에 적힌 옛날 버전을 억지로 맞추기보다, 그 시점 최신 안정판으로 시작하고 문서를 그에 맞게 갱신하는 게 낫다 (지금 그렇게 했다).

**Tailwind v3 → v4의 진짜 변화**: v3까지는 `tailwind.config.js`라는 JS 파일 안에 색상·폰트 같은 "테마"를 객체로 적었다. v4부터는 이 방식을 버리고, CSS 파일(`globals.css`) 안에 `@theme { --color-ink: #12253F; }` 처럼 **CSS 변수로 직접** 적는다. JS 설정 파일 자체가 필요 없어졌다 — "설정을 CSS 안으로 가져온다"는 큰 방향 전환이다.

---

## 4. 백엔드는 파이썬이 아니다 — 왜 파이썬을 쓰고 있었나

이 프로젝트의 실제 백엔드는 **없다(Phase 1)** → 나중에 앱 만들 때도 **Node.js + PostgreSQL**로 이미 정해져 있음(파이썬 아님).

그런데 실측 데이터(경로·고도)를 모으는 동안 파이썬을 썼던 이유: `scripts/build-profiles.ts`처럼 **한 번만 실행하고 끝나는 배치 스크립트**는 앱에 포함되지 않으니 어떤 언어로 짜든 상관없다. 파이썬이 API 호출 + 데이터 가공을 빠르게 실험하기 편해서 프로토타입으로 먼저 썼고, 검증되면 최종본은 TypeScript로 옮겨 적는다.

**핵심 구분**: "앱이 실행될 때 도는 코드"(TypeScript만) vs "데이터를 미리 만들어두는 일회성 도구"(아무 언어나 가능, 결과물인 데이터 파일만 앱에 들어감).

---

## 5. OpenStreetMap(OSM) 데이터를 실제로 어떻게 가져왔나

- **Overpass API**: OSM 데이터베이스에 질의(쿼리)를 던져서 원하는 지도 데이터만 뽑아오는 서비스. "Overpass QL"이라는 전용 질의어를 쓴다.
- **relation(관계)**: OSM에서 "이 길들을 순서대로 이으면 하나의 도보길이 된다"처럼 여러 요소를 묶는 단위. 카미노 프랑스 길은 **relation 하나가 아니라 6개 구간(relation)으로 쪼개져** 있었다 (생장→로그로뇨, 로그로뇨→부르고스, ...).
- **way(선)**: relation 안에 들어있는 실제 도로/오솔길 조각. 한 구간(relation)은 수백 개의 way로 이루어져 있고, **순서가 뒤섞여** 온다 — 그래서 "끝점이 서로 맞는 조각을 이어붙이는" 스티칭(stitching) 작업이 필요했다.
- **검증**: 이렇게 이어붙인 경로 길이가 163.3km 나왔는데, 원래 알고 있던 값(162.2km)과 거의 일치해서 "제대로 이었다"는 걸 확인할 수 있었다.

---

## 6. 고도 데이터는 왜 "IGN 5m 격자"가 아니라 "EU-DEM 25m"을 썼나

CLAUDE.md는 스페인 국립지리원(IGN)의 MDT05(5m 격자, 가장 정밀함)를 쓰라고 돼 있다. 그런데 IGN 데이터는 **공개 API가 아니라 직접 파일(GeoTIFF)을 다운로드해서 좌표계 변환까지 해야 하는** 훨씬 무거운 작업이다.

그 대신 **Open Topo Data**라는 무료 공개 API가 있고, 여기서 "EU-DEM 25m"(유럽 전역 25m 격자, CC-BY 라이선스)을 바로 조회할 수 있어서 먼저 이걸로 파이프라인 전체를 검증했다. **5m보다 거칠지만 "마을 고도차로 계산"하는 것보다는 압도적으로 정확하다.**

**검증 결과**: 생장→론세스바예스 구간 실제 상승고도를 1,327m로 계산했는데, CLAUDE.md에 이미 적혀있던 기준값(약 1,280m)과 4% 차이. 오차 원인 후보: (1) 25m 격자라 5m보다 거칢 (2) 스무딩(평활화) 방식 차이 (3) OSM에 그려진 실제 경로와 가이드북 경로가 살짝 다를 수 있음.

---

## 7. "노이즈 때문에 스무딩이 필수"라는 게 무슨 뜻인가

고도 데이터를 아무 가공 없이 이어 붙이면(raw), 지형 자체의 미세한 요철 + 측정 오차까지 전부 "오르막"으로 세어버려서 **실제보다 훨씬 크게 나온다.** (이번 검증에서 raw 1,513m vs 스무딩 후 1,327m — 스무딩 없이 그냥 더하면 14% 더 크게 나왔다.)

**스무딩 방법 두 가지를 같이 씀**:
1. **이동평균(moving average)**: 앞뒤 몇 개 지점의 고도를 평균 내서 뾰족한 튐을 눌러준다.
2. **히스테리시스(hysteresis) 임계값**: "3m 이상 방향이 바뀌어야 그제서야 오르막/내리막으로 인정"하는 규칙. 등산 앱들이 실제로 쓰는 표준적인 방식이다.

---

## 8. mermaid로 UML을 그린다는 것

UML(클래스 다이어그램 등)을 그림판으로 그리는 대신, **텍스트로 관계를 적으면 자동으로 그림이 되는 문법**이 mermaid다. 마크다운 문서 안에 ` ```mermaid ` 코드블록으로 넣어두면 GitHub나 특정 뷰어가 자동으로 그림으로 바꿔준다 (이 프로젝트의 `문서뷰어.html`은 지원 안 해서, 별도로 mermaid.js를 불러오는 `UML.html`을 따로 만들었다).

---

## 9. 이 맥에는 왜 Node를 새로 깔아야 했나 (Homebrew 이야기)

지난번 작업은 **윈도우 컴퓨터**에서 했고, 지금은 **맥**이다. 프로그램 설치는 컴퓨터마다 따로 해야 해서, 맥에는 Node.js(웹앱을 돌리는 엔진)가 아예 없었다.

- **Homebrew**: 맥에서 개발 도구를 설치·관리해 주는 "앱스토어 비슷한 것"(명령어로 쓰는). `brew install node` 한 줄로 Node를 깐다. 맥엔 이게 기본으로 없어서 Homebrew부터 설치했다.
- **왜 비밀번호를 직접 쳐야 했나?** Homebrew는 시스템 폴더를 만들어야 해서 관리자 권한(비밀번호)이 필요하다. 내(AI)가 붙어 있는 터미널은 비밀번호를 입력할 수 없어서, 그 한 줄만 사용자가 직접 쳤다.
- **결과**: node 26 설치 완료. 앞으로 이 맥에서 명령이 `node를 못 찾는다`고 하면 명령 앞에 경로(`/opt/homebrew/bin`)를 붙이면 된다.

## 10. "API가 진짜 값을 줬는지"를 어떻게 확인하나

우리는 인터넷의 두 무료 서비스(API)에서 데이터를 받아왔다: 길 모양(OpenStreetMap)과 땅 높이(고도). 문제는 **받아온 숫자가 진짜인지 그냥 그럴듯한 가짜인지** 어떻게 믿느냐다. 이 프로젝트는 사람 안전이 걸려 있어서(규칙 1) 특히 중요하다.

**확인 방법 = 이미 아는 사실과 대조.** 카미노에는 누구나 아는 유명한 지점들이 있다. 철의 십자가는 1,505m, 산티아고는 약 260m. 우리가 받은 데이터에서 그 지점 높이를 꺼내 보니 1,510m, 255m가 나왔다. **아는 값과 거의 똑같으면 → 진짜 데이터다.**

또 하나: **"전부 같은 값이면 가짜"**다. 높이 데이터의 들쭉날쭉한 정도(표준편차)가 250m로 나왔는데, 평평한 가짜 데이터면 0에 가깝다. 실제 산과 평야를 오르내리는 진짜 지형이라는 증거다.

**이번에 걸린 함정**: 확인하는 내 계산 공식에 실수가 있어서 처음엔 "다 틀렸다"고 나왔다. 데이터가 틀린 게 아니라 **검사 코드가 틀린** 것이었다. 교훈: 결과가 이상하면 데이터를 의심하기 전에 검사 방법부터 다시 본다.

## 11. "마을 위치를 어떻게 정했나" — 비율로 위치 찾기

길은 하나의 긴 선(767km)이고, 마을 82곳이 이 선 어디쯤 있는지 좌표(위도·경도)를 알아야 했다. 방법은 두 가지였다:

- (A) 마을 이름을 지도 검색에 넣어 좌표 받기 → 같은 이름 마을이 여러 개라 헷갈리고 오류 위험
- (B) **비율로 계산** → 채택. 가이드북에 "사리아는 658.9km 지점"처럼 이미 거리가 적혀 있다. 우리 실측 길이(767km)가 가이드북(773km)과 겨우 0.7% 차이라, "전체의 몇 %쯤 왔나"를 그대로 길 위에 대응시키면 위치가 나온다.

검증: 이렇게 찍은 팜플로나·부르고스·레온 좌표가 실제 도시 중심과 최대 1.1km 이내로 맞았다.

## 12. 왜 "마을 높이 빼기"로 오르막을 계산하면 안 되나 (핵심 규칙)

가장 중요한 규칙(규칙 3). 직관적으로는 "도착 마을 높이 − 출발 마을 높이 = 오르막"일 것 같다. **그런데 이게 사람을 다치게 한다.**

- 생장(170m) → 론세스바예스(950m)이면 단순 빼기는 "+780m"다. 그런데 실제로는 중간에 **1,450m 고개를 넘었다 내려온다.** 진짜 오르막은 1,300m. 빼기 방식은 이 고개를 통째로 놓친다.
- 더 위험한 건 내리막이다. 엘 아세보(1,145m) → 몰리나세카(595m)는 급경사 돌길로 무릎 부상이 많은데, 시작·끝 높이만 보면 그냥 "내려가는 날". 오르막 빼기 방식으론 위험이 안 보인다.

그래서 우리는 **길을 100m 간격으로 잘게 쪼개 매 지점 높이를 다 더한다.** 이 계산 결과만 `data/profiles.ts`에 담고, 앱은 여기서만 오르막·내리막을 읽는다. 코드가 실수로 "마을 높이 빼기"를 못 하도록 아예 테스트로 막아 뒀다.

## 13. "순수 함수"와 테스트가 왜 중요한가 (F-01 계획 엔진)

일정을 짜 주는 핵심 계산기(`lib/planner/`)를 만들었다. 여기엔 두 가지 원칙이 있다.

- **순수 함수**: "같은 입력이면 항상 같은 결과, 바깥세상을 건드리지 않는" 함수. 인터넷을 부르거나(`fetch`) 브라우저 기능(`window`)을 쓰지 않는다. 이유는 두 가지 — ① Phase 3에서 휴대폰 앱으로 **그대로 옮겨** 쓸 수 있고 ② 메세타·산속처럼 **인터넷이 끊긴 곳**에서도 계산이 돌아가야 하니까(규칙 10).
- **단위 테스트**: "생장에서 출발하면 하루 1개 도장, 사리아에서 출발하면 하루 2개"처럼 **정답을 미리 적어 두고 자동으로 맞는지 확인**하는 코드. 지금 23개를 넣어 뒀고, 나중에 코드를 고쳐도 이 23개가 다 통과하면 "안 망가졌다"고 믿을 수 있다.

**이번에 테스트가 실제로 버그를 잡았다**: 메세타를 버스로 건너뛰는 기능이, 하루 목표를 27km 이상으로 잡으면 조용히 무시되고 있었다. 걷기 목표에 따라 버스 출발 마을(부르고스)을 지나쳐 버렸기 때문. "출발 마을엔 반드시 멈춘다"로 고치고, 다시는 안 깨지게 테스트를 추가했다.

## 14. "도장 2개 규칙"은 위치가 아니라 거리 기준 — 자주 틀리는 도메인 지식

콤포스텔라(완주 증서)를 받으려면 마지막 100km를 걸어야 하고, 그 구간은 하루에 도장을 2개 받아야 한다. 많은 사람이 **"사리아를 지나면 2개"**라고 코드를 짠다. **틀렸다.**

기준은 **위치가 아니라 총 걸은 거리**다. 생장에서 773km를 걸어온 사람은 사리아를 지나도 하루 1개면 된다(최소 거리만 걷는 게 아니니까). 사리아에서 딱 114km만 걷는 사람만 2개다. 그래서 우리 엔진은 "어디서 출발해 총 몇 km 걷나"로 판단한다. (CLAUDE.md에도 이 함정이 별표로 적혀 있다.)

## 15. "더 정밀한 데이터가 더 정확한 건 아니다" — 5m vs 25m 고도 (반전)

우리는 높이 데이터를 25m 격자(EU-DEM)로 썼는데, 원래 계획은 더 촘촘한 **5m 격자(스페인 IGN)**를 쓰는 것이었다. "5m가 더 정밀하니 당연히 더 정확하겠지"라고 생각해서 실제로 IGN 5m를 받아 비교해 봤다. **결과는 정반대였다.**

- 평평하거나 완만한 구간(메세타, 오 세브레이로 등)은 5m든 25m든 거의 똑같았다(차이 10m 이내).
- 그런데 **가장 가파른 피레네 1일차**에서, 5m는 오르막을 +2,355m로 계산했다. 실제로 알려진 값은 약 1,280m인데 **거의 두 배로 부풀린 것**이다. 오히려 25m가 1,327m로 정답에 가까웠다.

**왜 이런 일이?** 비유하자면, 아주 성능 좋은 현미경으로 울퉁불퉁한 바위 표면을 재면 미세한 요철까지 다 잡힌다. 그런데 우리가 따라가는 "길 좌표"에는 원래 몇 m~몇십 m의 오차가 있다. 가파른 비탈에서는 옆으로 10m만 어긋나도 높이가 몇 m씩 튄다. 5m 데이터는 이 튐(노이즈)을 너무 충실히 담아서, 실제로는 없는 오르내림을 계속 더해 버린다. 25m 데이터는 애초에 넓게 뭉뚱그려 재기 때문에 이 노이즈가 자동으로 눌린다.

**교훈**: 부상 예측처럼 "길을 따라 오르막을 다 더하는" 계산에서는, 해상도가 높은 데이터가 오히려 독이 될 수 있다. 그래서 우리는 25m(EU-DEM)를 그대로 쓰기로 했다. (다만 데이터 출처 이름표는 실제와 맞게 고쳐야 한다 — 지금은 5m를 쓴 것처럼 적혀 있다.)

**"그럼 25m가 맞다는 건 또 어떻게 아나?"** — 이것도 좋은 질문이라 실제로 확인했다. 서로 완전히 다른 방식으로 만든 고도 데이터 3개(미국 레이더 SRTM, 미국·일본 위성 ASTER, 유럽 합성 EU-DEM)를 같은 지점에서 뽑아봤더니 **셋 다 1,309~1,327m 안에 모였다**(차이 18m). IGN 5m만 2,355m로 혼자 떨어졌다. 남이 만든 값(가이드북)을 안 믿어도, **독립된 여러 데이터가 한곳에 모이면 그게 신호이고 혼자 튀는 게 잡음**이라는 건 알 수 있다. 다만 "누적 오르막"에 완벽한 정답은 원래 없다 — 자를 잘게 댈수록 커지는 해안선처럼, 재는 방법에 따라 달라지기 때문이다.

## 16. 푸시도 풀도 할 게 있을 때 (브랜치 갈라짐) — 해결 절차

**증상**: IDE Git 버튼이 `From https://github.com/.../camino_planner` 까지만 뜨고 그다음 멈춘다. push도 안 된다.

**원인 두 가지가 겹친 것**:
1. **브랜치가 갈라졌다(diverged)**: 내 컴퓨터(로컬)에 원격에 없는 커밋이 있고(ahead), 동시에 원격(GitHub)에 내 로컬에 없는 커밋이 있다(behind). 예: 다른 데서 파일 하나 올려두고, 여기선 여러 커밋을 쌓은 상태.
2. **git이 합치는 방식을 모른다**: 최신 git은 갈라진 상태에서 `pull` 할 때 "리베이스로 합칠지, 머지로 합칠지" 를 안 정해두면 **거부**한다(`pull.rebase` 미설정). IDE의 Pull 버튼은 이 선택을 안 물어봐서 그냥 멈춰버린다.

**왜 push가 먼저 안 되나**: 원격에 내가 모르는 커밋이 있으면 git은 push를 막는다. 그걸 덮어쓰면 남의(=과거 내) 작업이 사라지니까. 그래서 **항상 "풀 먼저, 푸시 나중"**이다.

**해결 절차** (터미널에서 전략을 명시하면 IDE가 못 하던 걸 할 수 있다):
```bash
git status -sb                     # "ahead N, behind M" 로 갈라짐 확인
git pull --rebase origin main      # 원격 커밋 위에 내 커밋들을 다시 얹는다(한 줄로 깔끔)
git push origin main               # 이제 올라간다
git status -sb                     # "ahead/behind" 표시가 사라지면 완전 동기화 완료
```

- **리베이스 vs 머지**: `--rebase`는 내 커밋을 원격 것 위에 재배치해 역사가 한 줄로 깔끔. 그냥 `git pull`(머지)은 두 갈래를 잇는 "머지 커밋"이 하나 더 생긴다. 겹치는 파일이 없으면 어느 쪽이든 **충돌 없이** 된다.
- **충돌이 나면**: 같은 파일 같은 줄을 양쪽에서 고친 경우다. 표시된 부분(`<<<<<<<` ~ `>>>>>>>`)에서 남길 것을 고른 뒤 `git add 파일` → `git rebase --continue`. 꼬이면 `git rebase --abort`로 원상복구하고 다시 생각한다.
- **다음부터 IDE가 안 멈추게** (선택): `git config pull.rebase true`(이 저장소만) 또는 `--global`(전체). 그러면 Pull 버튼이 자동으로 리베이스 방식으로 동작한다.

**한 줄 요약**: 갈라짐 뜨면 → `git pull --rebase origin main` → `git push`. 끝.

---

